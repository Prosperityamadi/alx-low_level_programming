#include <stdio.h>

/**
 * main - prints all single digits numbers if b10
 *
 * Return:0 stops program
 *
 */

int main(void)
{
	int c;

	for (c = 0 ; c < 10 ; c++)
		printf("%d", c);
	printf("\n");
	return (0);

}
